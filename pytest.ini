[pytest]
testpaths = tests
markers =
    smoke: Quick smoke tests (page load, basic visibility)
    interaction: Tests involving user interactions (click, drag, type)
    panel: Tests for side panels (news panel, stats panel)
    search: Search functionality tests
    filter: Crisis filter tests
    history: History slider tests
    responsive: Responsiveness tests
addopts = -v --tb=short
